SESSION_SECRET=replace_with_long_random_string
# Legacy — superseded by the users table (src/auth.js, seeded via scripts/seed_users.js).
# No longer read by the login flow; kept only for reference.
MANAGER_PASSWORD=replace_with_shared_password
CLAUDE_API_KEY=sk-ant-replace_with_anthropic_key
PORT=3000
